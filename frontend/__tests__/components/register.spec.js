import Register from '../components/Register';

const register = Register;

test('Screen should render properly when loaded', () => {


  
})

test('When firstName field is filled out, state of firstName variable should change to entered text', () => {

  

})

test('When submit button is clicked submitHandler should dispatch info in state variables', () => {



})