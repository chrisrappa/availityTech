import React from 'react';
import { registerUser } from '../src/actions/userActions';

const userReducer = registerUser;

test('should return USER_REGISTER_REQUEST when intiated', () => {


})

test('should return USER_REGISTER_SUCCESS and payload: data when successfully provided user data', () => {
  
  
})

test('should return USER_REGISTER_FAIL and payload: error when not successfully provided user data', () => {
  

})