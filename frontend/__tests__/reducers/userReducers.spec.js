import userRegisterReducer from '../reducers/userReducer';

const userReducer = userRegisterReducer;

test('when action type is USER_REGISTER_REQUEST should return loading true', () => {


})

test('when action type is USER_REGISTER_SUCCESS should return loading false and userInfo', () => {
  
  
})


test('when action type is USER_REGISTER_FAIL should return loading false and error', () => {
  

})